# Verification downloads
downloads/

# Build outputs (anchored to repo root — an unanchored "wtop" also matches cmd/wtop/)
/wtop
/wtop.exe
/wtop-*.exe
*.bundle
*-sbom.cdx.json

# Windows resource objects — generated from cmd/wtop/winres/winres.json by
# go-winres (`go generate ./cmd/wtop/`), versioned from the release tag in CI
/cmd/wtop/*.syso

# Go test cache
*.test

# IDE / agent scratch
.vscode/
.idea/
/CLAUDE.md
*_qodana.sarif.json
